<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="com.sns.dao.ContentDelListMapper">
	
	<insert id="mtdContentDel" parameterType="com.sns.dto.ContentDelListDto">
		INSERT INTO content_del_list
		(del_id, content_id, manager, manager_id, reason)
		VALUES
		(#{del_id}, #{content_id}, #{manager}, #{}manager_id, #{reason})
	</insert>

</mapper>